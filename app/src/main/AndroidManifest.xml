<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.rod.skrin">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name="com.rod.skrin.MainActivity"></activity>
        <activity android:name="com.rod.skrin.MainEmptyActivity"
                android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name="com.rod.skrin.activities.ForgotPasswordActivity"
                android:windowSoftInputMode="adjustPan"></activity>
        <activity android:name="com.rod.skrin.activities.SignUpActivity">

        </activity>
        <activity
                android:name="com.rod.skrin.activities.LoginActivity"
                android:theme="@style/NoPreview"
                android:windowSoftInputMode="adjustPan"></activity>
    </application>

</manifest>